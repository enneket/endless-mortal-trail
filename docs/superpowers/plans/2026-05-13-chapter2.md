# 第二章「边关战火」实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现第二章的随机生成系统——骨架固定、NPC/物品随机抽取、运行时组装场景，并创建完整的边关战火故事内容。

**Architecture:** 新增 `SceneAssembler` 模块，负责从骨架 + NPC池 + 物品池运行时组装场景 JSON。`story-engine.js` 扩展支持检测章节类型（手写 vs 随机），自动调用组装器。场景模板使用 `{{NPC_SLOT}}` / `{{ITEM_SLOT}}` 占位符。

**Tech Stack:** HTML / CSS / JavaScript（无框架），JSON 数据文件

---

## 文件结构总览

```
story/chapter2/
├── meta.json                    # 章节元数据
├── skeleton.json                # 骨架定义
├── npcs.json                    # NPC 池（6个）
├── items.json                   # 物品池（5个）
└── templates/
    ├── 01_arrival.json           # 开局模板（含状态变体）
    ├── 02_camp.json              # 军营模板
    ├── 03_village.json           # 荒村模板
    ├── 04_fortress.json          # 将军府模板
    ├── 05_path_a.json            # 烽火台模板
    ├── 06_path_b.json            # 逃难路模板
    ├── 07_path_c.json            # 暗巷模板
    ├── 08_convergence.json       # 汇合模板
    ├── 09_revelation.json        # 真相模板
    ├── 10a_departure.json        # 离去结局
    ├── 10b_stay.json             # 留下结局
    └── 10c_resist.json           # 抗争结局
js/engine/
├── scene-assembler.js           # 新增：场景组装器
└── story-engine.js              # 修改：支持随机生成章节
```

---

## Task 1: NPC 池 + 物品池数据

**Files:**
- Create: `story/chapter2/npcs.json`
- Create: `story/chapter2/items.json`
- Create: `story/chapter2/meta.json`

- [ ] **Step 1: 创建 NPC 池**

```json
{
  "veteran": {
    "id": "veteran",
    "name": "老兵",
    "identity": "断臂老兵，守着废弃烽火台",
    "personality": "沉默、执拗、偶尔爆发",
    "dialogueStyle": "短句，军事术语，偶尔粗犷",
    "dialogues": {
      "greeting": "你站住。这里不欢迎外人。",
      "reveal": "仗打了三年。赢了，又输了。最后谁也没赢。",
      "gift": "拿着。这是我的军牌。留着也没用了。"
    }
  },
  "refugee_girl": {
    "id": "refugee_girl",
    "name": "流民少女",
    "identity": "逃难路上与家人失散的女孩",
    "personality": "警惕、倔强、内心柔软",
    "dialogueStyle": "简短、方言味、偶尔哽咽",
    "dialogues": {
      "greeting": "你……也是逃出来的？",
      "reveal": "我娘说，往南走就能活。可是南边也在打仗。",
      "gift": "这个香囊……是我娘绣的。你带着吧，保平安。"
    }
  },
  "strategist": {
    "id": "strategist",
    "name": "军师",
    "identity": "将军府中的幕僚",
    "personality": "冷静、算计、话中有话",
    "dialogueStyle": "文雅、暗藏机锋、喜欢引用典故",
    "dialogues": {
      "greeting": "哦？竟有人能找到这里来。",
      "reveal": "将军死了。但将军的死，不是战死的。",
      "gift": "这封信，你看了就明白。但看完之后，你最好忘掉。"
    }
  },
  "physician": {
    "id": "physician",
    "name": "郎中",
    "identity": "战地赤脚医生",
    "personality": "慈悲、疲惫、苦中作乐",
    "dialogueStyle": "温和、带药理比喻、自嘲",
    "dialogues": {
      "greeting": "来，坐下。你的脸色不好。",
      "reveal": "这仗打的不是人，是命。我救了三百人，没救下的……不数了。",
      "gift": "这药方你拿着。能救一人是一人。"
    }
  },
  "deserter": {
    "id": "deserter",
    "name": "逃兵",
    "identity": "从战场上跑出来的小兵",
    "personality": "恐惧、愧疚、想回家",
    "dialogueStyle": "断续、颤抖、偶尔语无伦次",
    "dialogues": {
      "greeting": "别……别杀我。我不是坏人。",
      "reveal": "我跑了。我亲眼看着他们死的，我跑了。",
      "gift": "这封信……我本来要寄回家的。你帮我……帮我寄出去好不好？"
    }
  },
  "general_widow": {
    "id": "general_widow",
    "name": "将军遗孀",
    "identity": "已故将军的夫人",
    "personality": "高傲、悲伤、决绝",
    "dialogueStyle": "古雅、克制、偶尔锋利",
    "dialogues": {
      "greeting": "你来了。我等了很久了。",
      "reveal": "将军不是死在战场上的。他是被人害死的。",
      "ending": "走吧。这里的事，与你无关。但你若想留下……我也不拦你。"
    }
  }
}
```

- [ ] **Step 2: 创建物品池**

```json
{
  "military_tag": {
    "id": "military_tag",
    "name": "军牌",
    "description": "一枚铜制军牌，刻着一个已经模糊的名字。握在手中，能感受到主人最后的执念。",
    "effects": { "setFlag": "has_military_tag" }
  },
  "embroidered_pouch": {
    "id": "embroidered_pouch",
    "name": "绣囊",
    "description": "粗布绣成的香囊，针脚歪斜却用心。里面装着干枯的艾草，是家乡的味道。",
    "effects": { "setFlag": "has_embroidered_pouch" }
  },
  "secret_letter": {
    "id": "secret_letter",
    "name": "密信",
    "description": "一封用暗语写成的信，字迹工整。上面盖着一个你认不出的印章。",
    "effects": { "setFlag": "has_secret_letter" }
  },
  "prescription": {
    "id": "prescription",
    "name": "药方",
    "description": "一张发黄的药方，上面的药名你大多不认识。角落里写着一行小字：'此方可救一人，亦可误一人。'",
    "effects": { "setFlag": "has_prescription" }
  },
  "letter_home": {
    "id": "letter_home",
    "name": "家书",
    "description": "一封没寄出的家书。'娘，孩儿不孝……'后面的字被泪水洇开了。",
    "effects": { "setFlag": "has_letter_home" }
  }
}
```

- [ ] **Step 3: 创建章节元数据**

```json
{
  "id": "chapter2",
  "title": "边关战火",
  "description": "渡途人踏入战火纷飞的边关人间",
  "sceneCount": 12,
  "startScene": "01_arrival",
  "randomized": true
}
```

- [ ] **Step 4: 验证 JSON**

```bash
python3 -c "import json; json.load(open('story/chapter2/npcs.json')); json.load(open('story/chapter2/items.json')); json.load(open('story/chapter2/meta.json')); print('All valid')"
```

- [ ] **Step 5: 提交**

```bash
git add story/chapter2/npcs.json story/chapter2/items.json story/chapter2/meta.json
git commit -m "feat(ch2): add NPC pool, item pool, and chapter metadata"
```

---

## Task 2: 骨架定义

**Files:**
- Create: `story/chapter2/skeleton.json`

- [ ] **Step 1: 创建骨架文件**

```json
{
  "chapterId": "chapter2",
  "title": "边关战火",
  "startScene": "01_arrival",
  "npcSlots": {
    "slot_1": { "candidates": ["veteran", "refugee_girl"], "scene": "02_camp" },
    "slot_2": { "candidates": ["refugee_girl", "physician"], "scene": "03_village" },
    "slot_3": { "candidates": ["strategist", "deserter"], "scene": "04_fortress" },
    "slot_4": { "candidates": ["veteran", "deserter"], "scene": "05_path_a" },
    "slot_5": { "candidates": ["refugee_girl", "physician"], "scene": "06_path_b" },
    "slot_6": { "candidates": ["strategist", "deserter"], "scene": "07_path_c" }
  },
  "itemSlots": {
    "slot_a": { "candidates": ["military_tag", "letter_home"], "scene": "05_path_a" },
    "slot_b": { "candidates": ["embroidered_pouch", "prescription"], "scene": "06_path_b" },
    "slot_c": { "candidates": ["secret_letter", "letter_home"], "scene": "07_path_c" }
  },
  "scenes": [
    "01_arrival",
    "02_camp",
    "03_village",
    "04_fortress",
    "05_path_a",
    "06_path_b",
    "07_path_c",
    "08_convergence",
    "09_revelation",
    "10a_departure",
    "10b_stay",
    "10c_resist"
  ]
}
```

- [ ] **Step 2: 提交**

```bash
git add story/chapter2/skeleton.json
git commit -m "feat(ch2): add skeleton definition with NPC and item slots"
```

---

## Task 3: 场景组装器

**Files:**
- Create: `js/engine/scene-assembler.js`

- [ ] **Step 1: 创建场景组装器模块**

```javascript
/**
 * scene-assembler.js - 场景组装器
 * 人间无尽途 · 随机生成核心
 *
 * 从骨架 + NPC池 + 物品池运行时组装完整场景 JSON。
 * 模板中使用 {{NPC_SLOT_N:field}} 和 {{ITEM_SLOT_X:field}} 占位符。
 */

export class SceneAssembler {
  /**
   * @param {object} skeleton - 骨架定义
   * @param {object} npcPool - NPC 池
   * @param {object} itemPool - 物品池
   */
  constructor(skeleton, npcPool, itemPool) {
    this.skeleton = skeleton;
    this.npcPool = npcPool;
    this.itemPool = itemPool;

    /** @type {Record<string, string>} slot → npcId */
    this.npcAssignments = {};
    /** @type {Record<string, string>} slot → itemId */
    this.itemAssignments = {};

    this._assignSlots();
  }

  // ---------------------------------------------------------------------------
  // 槽位分配
  // ---------------------------------------------------------------------------

  /**
   * 从候选池中随机抽取，分配到各槽位。
   * 确保同一个 NPC 不会被分配到多个槽位。
   */
  _assignSlots() {
    const usedNpcs = new Set();
    const usedItems = new Set();

    for (const [slotId, slot] of Object.entries(this.skeleton.npcSlots)) {
      const available = slot.candidates.filter(id => !usedNpcs.has(id));
      const picked = this._pickRandom(available);
      this.npcAssignments[slotId] = picked;
      if (picked) usedNpcs.add(picked);
    }

    for (const [slotId, slot] of Object.entries(this.skeleton.itemSlots)) {
      const available = slot.candidates.filter(id => !usedItems.has(id));
      const picked = this._pickRandom(available);
      this.itemAssignments[slotId] = picked;
      if (picked) usedItems.add(picked);
    }
  }

  /**
   * 从数组中随机选取一个元素。
   * @param {Array} arr
   * @returns {*|null}
   */
  _pickRandom(arr) {
    if (!arr || arr.length === 0) return null;
    return arr[Math.floor(Math.random() * arr.length)];
  }

  // ---------------------------------------------------------------------------
  // 场景组装
  // ---------------------------------------------------------------------------

  /**
   * 组装指定场景的完整 JSON。
   * @param {string} sceneId - 场景 ID
   * @param {object} template - 场景模板
   * @param {object} gameState - 当前游戏状态（用于状态继承）
   * @returns {object} 完整的场景 JSON
   */
  assemble(sceneId, template, gameState) {
    const scene = JSON.parse(JSON.stringify(template));

    // 替换文本中的占位符
    if (scene.texts) {
      scene.texts = scene.texts.map(entry => ({
        ...entry,
        content: this._replacePlaceholders(entry.content, gameState)
      }));
    }

    // 替换选项中的占位符
    if (scene.choices) {
      scene.choices = scene.choices.map(choice => ({
        ...choice,
        text: this._replacePlaceholders(choice.text, gameState)
      }));

      // 过滤掉条件不满足的选项
      scene.choices = scene.choices.filter(choice => {
        if (!choice.condition) return true;
        return this._checkCondition(choice.condition, gameState);
      });
    }

    // 注入物品获取效果
    this._injectItemEffects(scene);

    return scene;
  }

  // ---------------------------------------------------------------------------
  // 占位符替换
  // ---------------------------------------------------------------------------

  /**
   * 替换文本中的 {{SLOT:field}} 占位符。
   *
   * 支持的占位符：
   *   {{NPC_SLOT_N:name}} - NPC 槽位的名称
   *   {{NPC_SLOT_N:dialogue_type}} - NPC 对话
   *   {{ITEM_SLOT_X:name}} - 物品名称
   *   {{ITEM_SLOT_X:description}} - 物品描述
   *   {{STATE:mood}} - 当前心境
   *   {{STATE:flag_name}} - 标记存在时替换为变体文本
   *
   * @param {string} text
   * @param {object} gameState
   * @returns {string}
   */
  _replacePlaceholders(text, gameState) {
    if (!text) return text;

    // NPC 占位符
    text = text.replace(/\{\{NPC_SLOT_(\d+):(\w+)\}\}/g, (match, slotNum, field) => {
      const slotId = `slot_${slotNum}`;
      const npcId = this.npcAssignments[slotId];
      if (!npcId) return match;
      const npc = this.npcPool[npcId];
      if (!npc) return match;

      if (field === 'name') return npc.name;
      if (field === 'identity') return npc.identity;
      if (npc.dialogues && npc.dialogues[field]) return npc.dialogues[field];
      return match;
    });

    // 物品占位符
    text = text.replace(/\{\{ITEM_SLOT_([a-c]):(\w+)\}\}/g, (match, slotLetter, field) => {
      const slotId = `slot_${slotLetter}`;
      const itemId = this.itemAssignments[slotId];
      if (!itemId) return match;
      const item = this.itemPool[itemId];
      if (!item) return match;

      if (field === 'name') return item.name;
      if (field === 'description') return item.description;
      return match;
    });

    // 状态占位符
    text = text.replace(/\{\{STATE:(\w+)\}\}/g, (match, key) => {
      if (!gameState) return match;

      if (key === 'mood') return gameState.get('stats.mood') || '迷茫';

      // 标记检查：如果标记存在，返回空字符串（移除变体文本）
      if (gameState.hasFlag(key)) return '';
      return match;
    });

    return text;
  }

  // ---------------------------------------------------------------------------
  // 条件检查
  // ---------------------------------------------------------------------------

  /**
   * 检查条件是否满足。
   * @param {object} condition
   * @param {object} gameState
   * @returns {boolean}
   */
  _checkCondition(condition, gameState) {
    if (!condition || !gameState) return true;

    if (condition.hasItem && !gameState.hasItem(condition.hasItem)) return false;
    if (condition.hasFlag && !gameState.hasFlag(condition.hasFlag)) return false;
    if (condition.minKarma !== undefined && gameState.get('stats.karma') < condition.minKarma) return false;
    if (condition.mood && gameState.get('stats.mood') !== condition.mood) return false;

    return true;
  }

  // ---------------------------------------------------------------------------
  // 物品注入
  // ---------------------------------------------------------------------------

  /**
   * 将分配的物品效果注入到场景选项中。
   * @param {object} scene
   */
  _injectItemEffects(scene) {
    if (!scene.choices) return;

    for (const choice of scene.choices) {
      if (choice._itemSlot) {
        const slotId = `slot_${choice._itemSlot}`;
        const itemId = this.itemAssignments[slotId];
        if (itemId) {
          const item = this.itemPool[itemId];
          if (item && item.effects) {
            choice.effects = { ...choice.effects, ...item.effects, addItem: itemId };
          }
        }
        delete choice._itemSlot;
      }
    }
  }

  // ---------------------------------------------------------------------------
  // 查询
  // ---------------------------------------------------------------------------

  /**
   * 获取指定槽位分配的 NPC ID。
   * @param {string} slotId
   * @returns {string|null}
   */
  getNpcForSlot(slotId) {
    return this.npcAssignments[slotId] || null;
  }

  /**
   * 获取指定槽位分配的物品 ID。
   * @param {string} slotId
   * @returns {string|null}
   */
  getItemForSlot(slotId) {
    return this.itemAssignments[slotId] || null;
  }

  /**
   * 获取指定 NPC 的完整数据。
   * @param {string} npcId
   * @returns {object|null}
   */
  getNpcData(npcId) {
    return this.npcPool[npcId] || null;
  }
}
```

- [ ] **Step 2: 验证语法**

```bash
node --check js/engine/scene-assembler.js
```

- [ ] **Step 3: 提交**

```bash
git add js/engine/scene-assembler.js
git commit -m "feat(ch2): add scene assembler with NPC/item slot injection"
```

---

## Task 4: 场景模板（01-04 开局 + 探索入口）

**Files:**
- Create: `story/chapter2/templates/01_arrival.json`
- Create: `story/chapter2/templates/02_camp.json`
- Create: `story/chapter2/templates/03_village.json`
- Create: `story/chapter2/templates/04_fortress.json`

- [ ] **Step 1: 创建 01_arrival 模板**

```json
{
  "id": "01_arrival",
  "title": "初入边关",
  "atmosphere": { "time": "黄昏", "weather": "硝烟", "mood": "苍凉" },
  "texts": [
    {
      "content": "裂缝在身后合拢。你站在一条黄土路上，脚下是龟裂的土地，头顶是灰蒙蒙的天。空气里弥漫着焦木和铁锈的气味——是火，是血，是某种已经结束了的东西。",
      "delay": 0
    },
    {
      "content": "路的两旁是烧焦的田地。稻谷早已收割——或者被烧光了，你分不清。远处有几缕青烟升起，不是炊烟，是废墟在闷烧。一只乌鸦站在路边的枯树上，歪着头看你，没有叫。",
      "delay": 3000
    },
    {
      "content": "你向前走。黄土路上有脚印，很多脚印，密密麻麻，朝着同一个方向。有的脚印很深，像是背着沉重的东西；有的很浅，像是在跑。你沿着脚印走，不知道会走到哪里。",
      "delay": 3500
    }
  ],
  "choices": [
    {
      "text": "沿着脚印向前走",
      "target": "02_camp",
      "effects": { "experience": 1 },
      "condition": null
    },
    {
      "text": "拐进旁边的岔路，去看看青烟升起的地方",
      "target": "03_village",
      "effects": { "wisdom": 1 },
      "condition": null
    },
    {
      "text": "远处隐约可见一座府邸的轮廓",
      "target": "04_fortress",
      "effects": { "courage": 1 },
      "condition": null
    }
  ],
  "onEnter": [],
  "onExit": []
}
```

- [ ] **Step 2: 创建 02_camp 模板**

```json
{
  "id": "02_camp",
  "title": "军营残迹",
  "atmosphere": { "time": "黄昏", "weather": "阴沉", "mood": "沉重" },
  "texts": [
    {
      "content": "你走到一片废弃的军营。帐篷大多已经倒塌，只剩下几根孤零零的旗杆，上面挂着破烂的旗帜，被风吹得猎猎作响。地上散落着断矛、碎盾、还有已经干涸发黑的血迹。",
      "delay": 0
    },
    {
      "content": "你在废墟中穿行，忽然听见一个声音。",
      "delay": 2000
    },
    {
      "content": "{{NPC_SLOT_1:greeting}}",
      "delay": 1500
    },
    {
      "content": "你转过身，看见了{{NPC_SLOT_1:name}}。{{NPC_SLOT_1:identity}}。",
      "delay": 1000
    },
    {
      "content": "{{NPC_SLOT_1:reveal}}",
      "delay": 2000
    }
  ],
  "choices": [
    {
      "text": ""我想了解更多。"",
      "target": "05_path_a",
      "effects": { "wisdom": 1, "setFlag": "sympathized" },
      "condition": null
    },
    {
      "text": ""这不关我的事。"",
      "target": "05_path_a",
      "effects": { "mood": "淡然" },
      "condition": null
    }
  ],
  "onEnter": [],
  "onExit": []
}
```

- [ ] **Step 3: 创建 03_village 模板**

```json
{
  "id": "03_village",
  "title": "战后荒村",
  "atmosphere": { "time": "黄昏", "weather": "薄雾", "mood": "诡异" },
  "texts": [
    {
      "content": "你沿着岔路走进一个村庄。或者说，曾经是一个村庄。现在只剩下断壁残垣和满地的瓦砾。墙上有箭矢留下的孔洞，地上有被踩碎的陶罐和散落的衣物。",
      "delay": 0
    },
    {
      "content": "雾气从废墟间升起，带着一种潮湿的腐朽气味。你在废墟中走着，忽然看见一个人影。",
      "delay": 2500
    },
    {
      "content": "{{NPC_SLOT_2:greeting}}",
      "delay": 1500
    },
    {
      "content": "是{{NPC_SLOT_2:name}}。{{NPC_SLOT_2:identity}}。",
      "delay": 1000
    },
    {
      "content": "{{NPC_SLOT_2:reveal}}",
      "delay": 2000
    }
  ],
  "choices": [
    {
      "text": ""你还好吗？"",
      "target": "06_path_b",
      "effects": { "karma": 1, "setFlag": "sympathized" },
      "condition": null
    },
    {
      "text": "默默离开",
      "target": "06_path_b",
      "effects": { "mood": "淡然" },
      "condition": null
    }
  ],
  "onEnter": [],
  "onExit": []
}
```

- [ ] **Step 4: 创建 04_fortress 模板**

```json
{
  "id": "04_fortress",
  "title": "将军府",
  "atmosphere": { "time": "深夜", "weather": "月光", "mood": "肃杀" },
  "texts": [
    {
      "content": "你走到一座府邸前。大门半开，门上的铜钉已经生锈。门前的石狮子缺了半个头，像是被什么利器劈开的。你推门进去，院子里杂草丛生，正厅的门楣上挂着一块匾，上面的字已经被刮掉了。",
      "delay": 0
    },
    {
      "content": "正厅里空荡荡的，只有一张案几和一把太师椅。案几上放着一壶酒，酒壶旁边是一封拆开的信。墙上挂着一幅画像——一个身穿铠甲的将军，面容刚毅，眼神里有几分疲惫。",
      "delay": 3000
    },
    {
      "content": "你听见身后有脚步声。",
      "delay": 1500
    },
    {
      "content": "{{NPC_SLOT_3:greeting}}",
      "delay": 1000
    },
    {
      "content": "是{{NPC_SLOT_3:name}}。{{NPC_SLOT_3:identity}}。",
      "delay": 1000
    },
    {
      "content": "{{NPC_SLOT_3:reveal}}",
      "delay": 2000
    }
  ],
  "choices": [
    {
      "text": ""将军是怎么死的？"",
      "target": "07_path_c",
      "effects": { "setFlag": "defied", "wisdom": 1 },
      "condition": null
    },
    {
      "text": ""我该走了。"",
      "target": "07_path_c",
      "effects": { "mood": "淡然" },
      "condition": null
    }
  ],
  "onEnter": [],
  "onExit": []
}
```

- [ ] **Step 5: 验证 JSON**

```bash
for f in story/chapter2/templates/01_arrival.json story/chapter2/templates/02_camp.json story/chapter2/templates/03_village.json story/chapter2/templates/04_fortress.json; do python3 -c "import json; json.load(open('$f')); print('OK: $f')"; done
```

- [ ] **Step 6: 提交**

```bash
git add story/chapter2/templates/01_arrival.json story/chapter2/templates/02_camp.json story/chapter2/templates/03_village.json story/chapter2/templates/04_fortress.json
git commit -m "feat(ch2): add scene templates 01-04 (arrival + exploration entries)"
```

---

## Task 5: 场景模板（05-07 探索路径）

**Files:**
- Create: `story/chapter2/templates/05_path_a.json`
- Create: `story/chapter2/templates/06_path_b.json`
- Create: `story/chapter2/templates/07_path_c.json`

- [ ] **Step 1: 创建 05_path_a 模板**

```json
{
  "id": "05_path_a",
  "title": "烽火台",
  "atmosphere": { "time": "黎明", "weather": "寒风", "mood": "孤寂" },
  "texts": [
    {
      "content": "你爬上一座废弃的烽火台。台阶很陡，石阶上有刀砍的痕迹。登上台顶，寒风扑面而来，你看见了整个边关的全貌——荒芜的田野，倒塌的村庄，还有远处蜿蜒的长城，像一条断了脊梁的巨蛇。",
      "delay": 0
    },
    {
      "content": "{{NPC_SLOT_4:greeting}}",
      "delay": 2500
    },
    {
      "content": "{{NPC_SLOT_4:name}}站在烽火台的另一端。{{NPC_SLOT_4:identity}}。",
      "delay": 1000
    },
    {
      "content": "{{NPC_SLOT_4:reveal}}",
      "delay": 2000
    },
    {
      "content": "你注意到烽火台的角落里有一个东西。",
      "delay": 1500
    },
    {
      "content": "得到：{{ITEM_SLOT_A:name}}",
      "delay": 500
    }
  ],
  "choices": [
    {
      "text": "收好东西，继续前行",
      "target": "08_convergence",
      "effects": {},
      "condition": null,
      "_itemSlot": "a"
    }
  ],
  "onEnter": [],
  "onExit": []
}
```

- [ ] **Step 2: 创建 06_path_b 模板**

```json
{
  "id": "06_path_b",
  "title": "逃难路",
  "atmosphere": { "time": "白昼", "weather": "烈日", "mood": "压抑" },
  "texts": [
    {
      "content": "你走上一条黄土大路。路上挤满了人——老人、孩子、妇人，背着包袱，拄着拐杖，默默地向南走。没有人说话，只有脚步声和偶尔的咳嗽声。路旁倒着几个人，不知道是睡着了还是死了。没有人停下来。",
      "delay": 0
    },
    {
      "content": "{{NPC_SLOT_5:greeting}}",
      "delay": 3000
    },
    {
      "content": "{{NPC_SLOT_5:name}}。{{NPC_SLOT_5:identity}}。",
      "delay": 1000
    },
    {
      "content": "{{NPC_SLOT_5:reveal}}",
      "delay": 2000
    },
    {
      "content": "{{NPC_SLOT_5:gift}}",
      "delay": 1500
    },
    {
      "content": "得到：{{ITEM_SLOT_B:name}}",
      "delay": 500
    }
  ],
  "choices": [
    {
      "text": "接过东西，继续前行",
      "target": "08_convergence",
      "effects": {},
      "condition": null,
      "_itemSlot": "b"
    }
  ],
  "onEnter": [],
  "onExit": []
}
```

- [ ] **Step 3: 创建 07_path_c 模板**

```json
{
  "id": "07_path_c",
  "title": "暗巷",
  "atmosphere": { "time": "深夜", "weather": "无月", "mood": "紧张" },
  "texts": [
    {
      "content": "你走进城中的一条暗巷。巷子很窄，两侧的墙壁几乎可以同时用手触到。墙上没有窗，只有偶尔从门缝里透出的微弱灯光。脚下的石板路湿漉漉的，不知道是雨水还是别的什么。",
      "delay": 0
    },
    {
      "content": "你走了很久，来到巷子的尽头。那里有一扇半掩的门，门里传出低低的说话声。",
      "delay": 2500
    },
    {
      "content": "{{NPC_SLOT_6:greeting}}",
      "delay": 1500
    },
    {
      "content": "是{{NPC_SLOT_6:name}}。{{NPC_SLOT_6:identity}}。",
      "delay": 1000
    },
    {
      "content": "{{NPC_SLOT_6:reveal}}",
      "delay": 2000
    },
    {
      "content": "{{NPC_SLOT_6:gift}}",
      "delay": 1500
    },
    {
      "content": "得到：{{ITEM_SLOT_C:name}}",
      "delay": 500
    }
  ],
  "choices": [
    {
      "text": "收好东西，继续前行",
      "target": "08_convergence",
      "effects": {},
      "condition": null,
      "_itemSlot": "c"
    }
  ],
  "onEnter": [],
  "onExit": []
}
```

- [ ] **Step 4: 验证 JSON**

```bash
for f in story/chapter2/templates/05_path_a.json story/chapter2/templates/06_path_b.json story/chapter2/templates/07_path_c.json; do python3 -c "import json; json.load(open('$f')); print('OK: $f')"; done
```

- [ ] **Step 5: 提交**

```bash
git add story/chapter2/templates/05_path_a.json story/chapter2/templates/06_path_b.json story/chapter2/templates/07_path_c.json
git commit -m "feat(ch2): add scene templates 05-07 (exploration paths with item slots)"
```

---

## Task 6: 场景模板（08-10 汇合 + 结局）

**Files:**
- Create: `story/chapter2/templates/08_convergence.json`
- Create: `story/chapter2/templates/09_revelation.json`
- Create: `story/chapter2/templates/10a_departure.json`
- Create: `story/chapter2/templates/10b_stay.json`
- Create: `story/chapter2/templates/10c_resist.json`

- [ ] **Step 1: 创建 08_convergence 模板**

```json
{
  "id": "08_convergence",
  "title": "汇合",
  "atmosphere": { "time": "黎明", "weather": "薄雾", "mood": "沉重" },
  "texts": [
    {
      "content": "三条路在一座破旧的祠堂前汇合。祠堂的门开着，里面点着一盏油灯，灯光昏黄，在风中摇曳。门槛上坐着一个女人，穿着素白的衣裳，头发挽成一个简单的髻，面容清冷，眼神里有一种说不出的哀伤。",
      "delay": 0
    },
    {
      "content": ""你来了。"她说，声音很轻，像是从很远的地方传来。"我等了很久了。"",
      "delay": 2500
    },
    {
      "content": "她是将军遗孀。",
      "delay": 1000
    },
    {
      "content": ""将军死了。但将军的死，不是战死的。"",
      "delay": 2000
    },
    {
      "content": "她看着你，眼神里有一种决绝。",
      "delay": 1000
    },
    {
      "content": ""你想知道真相吗？"",
      "delay": 1500
    }
  ],
  "choices": [
    {
      "text": ""我想知道。"",
      "target": "09_revelation",
      "effects": { "wisdom": 1 },
      "condition": null
    },
    {
      "text": ""我不想卷入这些事。"",
      "target": "10a_departure",
      "effects": { "mood": "淡然" },
      "condition": null
    }
  ],
  "onEnter": [],
  "onExit": []
}
```

- [ ] **Step 2: 创建 09_revelation 模板**

```json
{
  "id": "09_revelation",
  "title": "真相大白",
  "atmosphere": { "time": "黎明", "weather": "晴", "mood": "肃杀" },
  "texts": [
    {
      "content": "将军遗孀从怀里掏出一封信，递给你。",
      "delay": 0
    },
    {
      "content": ""这是将军临死前写的。他知道自己会死，但他没有逃。"",
      "delay": 1500
    },
    {
      "content": "你打开信。信上只有几行字：'吾一生征战，杀人无数。今日被人所杀，亦是因果。唯愿后人记住——战争没有赢家。'",
      "delay": 2000
    },
    {
      "content": ""将军是被朝廷的人害死的。"她说，声音很平静。"因为他主张议和。"",
      "delay": 2000
    },
    {
      "content": "你沉默了。风吹过祠堂，油灯摇曳了一下。",
      "delay": 1500
    },
    {
      "content": ""现在你知道了。"她看着你。"你想怎么做？"",
      "delay": 1500
    }
  ],
  "choices": [
    {
      "text": "离开这里，继续我的旅途",
      "target": "10a_departure",
      "effects": { "mood": "淡然" },
      "condition": null
    },
    {
      "text": "留下来，试着做些什么",
      "target": "10b_stay",
      "effects": { "karma": 2, "setFlag": "sympathized" },
      "condition": { "hasFlag": "sympathized" }
    },
    {
      "text": "我要让真相大白于天下",
      "target": "10c_resist",
      "effects": { "courage": 2, "setFlag": "defied" },
      "condition": { "hasFlag": "defied" }
    }
  ],
  "onEnter": [],
  "onExit": []
}
```

- [ ] **Step 3: 创建 10a_departure 模板**

```json
{
  "id": "10a_departure",
  "title": "离去",
  "atmosphere": { "time": "白昼", "weather": "晴", "mood": "平静" },
  "texts": [
    {
      "content": "你转身离开了祠堂。将军遗孀没有拦你，只是静静地看着你走远。",
      "delay": 0
    },
    {
      "content": "你走回那条黄土路。路上的流民已经少了很多，只剩下几个走不动的老人，坐在路边发呆。",
      "delay": 2000
    },
    {
      "content": "你继续走着。身后是战火，身前是未知。",
      "delay": 1500
    },
    {
      "content": "你没有回头。",
      "delay": 1000
    },
    {
      "content": "——第二章·完——",
      "delay": 1500
    }
  ],
  "choices": [
    {
      "text": "继续旅途（待续）",
      "target": "01_arrival",
      "effects": { "experience": 3 },
      "condition": null
    }
  ],
  "onEnter": [],
  "onExit": []
}
```

- [ ] **Step 4: 创建 10b_stay 模板**

```json
{
  "id": "10b_stay",
  "title": "留下",
  "atmosphere": { "time": "黄昏", "weather": "余晖", "mood": "温暖" },
  "texts": [
    {
      "content": ""我留下。"你说。",
      "delay": 0
    },
    {
      "content": "将军遗孀看着你，眼里闪过一丝惊讶，随即化为一种复杂的笑意。",
      "delay": 1500
    },
    {
      "content": ""好。"她说。"那你帮我一个忙。"",
      "delay": 1000
    },
    {
      "content": "她带你走进祠堂后面的小院。院子里有一口井，井水很清。她从井里打了一桶水，递给你一碗。",
      "delay": 2000
    },
    {
      "content": ""喝吧。这是边关最后一口干净的井。"",
      "delay": 1500
    },
    {
      "content": "你喝了一口。水是甜的。",
      "delay": 1000
    },
    {
      "content": "你决定留下来，做你能做的事。也许不多，但至少不是什么都不做。",
      "delay": 2000
    },
    {
      "content": "——第二章·完——",
      "delay": 1500
    }
  ],
  "choices": [
    {
      "text": "继续旅途（待续）",
      "target": "01_arrival",
      "effects": { "experience": 3 },
      "condition": null
    }
  ],
  "onEnter": [],
  "onExit": []
}
```

- [ ] **Step 5: 创建 10c_resist 模板**

```json
{
  "id": "10c_resist",
  "title": "抗争",
  "atmosphere": { "time": "黎明", "weather": "狂风", "mood": "壮烈" },
  "texts": [
    {
      "content": ""我要让真相大白于天下。"你说。",
      "delay": 0
    },
    {
      "content": "将军遗孀看着你，眼里有泪光闪过。",
      "delay": 1500
    },
    {
      "content": ""你疯了。"她说。"朝廷的人会杀了你。"",
      "delay": 1000
    },
    {
      "content": ""那又怎样？"你说。",
      "delay": 800
    },
    {
      "content": "她沉默了很久。",
      "delay": 1000
    },
    {
      "content": ""好。"她终于说。"我把将军的信给你。你带着它，走遍天下。"",
      "delay": 1500
    },
    {
      "content": "你接过信。信纸已经泛黄，但字迹依然清晰。",
      "delay": 1000
    },
    {
      "content": "你转身走进风里。身后是边关，身前是天下。你知道这条路很难，但你没有犹豫。",
      "delay": 2000
    },
    {
      "content": "——第二章·完——",
      "delay": 1500
    }
  ],
  "choices": [
    {
      "text": "继续旅途（待续）",
      "target": "01_arrival",
      "effects": { "experience": 3 },
      "condition": null
    }
  ],
  "onEnter": [],
  "onExit": []
}
```

- [ ] **Step 6: 验证 JSON**

```bash
for f in story/chapter2/templates/08_convergence.json story/chapter2/templates/09_revelation.json story/chapter2/templates/10a_departure.json story/chapter2/templates/10b_stay.json story/chapter2/templates/10c_resist.json; do python3 -c "import json; json.load(open('$f')); print('OK: $f')"; done
```

- [ ] **Step 7: 提交**

```bash
git add story/chapter2/templates/
git commit -m "feat(ch2): add scene templates 08-10 (convergence + endings)"
```

---

## Task 7: 扩展 story-engine 支持随机生成

**Files:**
- Modify: `js/engine/story-engine.js`

- [ ] **Step 1: 导入 SceneAssembler**

在文件顶部添加导入：

```javascript
import { SceneAssembler } from './scene-assembler.js';
```

- [ ] **Step 2: 在构造函数中添加组装器字段**

```javascript
constructor(gameState, renderer, saveManager) {
  this.gameState = gameState;
  this.renderer = renderer;
  this.saveManager = saveManager;

  /** @type {Record<string, object>} */
  this.sceneCache = {};
  /** @type {Record<string, object>} */
  this.npcData = {};
  /** @type {Record<string, object>} */
  this.itemData = {};
  /** @type {object | null} */
  this.chapterMeta = null;
  /** @type {SceneAssembler | null} */
  this.assembler = null;
}
```

- [ ] **Step 3: 修改 loadChapter 支持随机生成**

```javascript
async loadChapter(chapterId) {
  const base = `story/${chapterId}`;

  const meta = await this._fetchJSON(`${base}/meta.json`);
  this.chapterMeta = meta;

  if (meta && meta.randomized) {
    // 随机生成章节：加载骨架、NPC 池、物品池
    const [skeleton, npcs, items] = await Promise.all([
      this._fetchJSON(`${base}/skeleton.json`),
      this._fetchJSON(`${base}/npcs.json`),
      this._fetchJSON(`${base}/items.json`),
    ]);

    this.assembler = new SceneAssembler(skeleton, npcs ?? {}, items ?? {});
    this.npcData = npcs ?? {};
    this.itemData = items ?? {};
  } else {
    // 手写章节：加载共享 NPC/物品数据
    const [npcs, items] = await Promise.all([
      this._fetchJSON('story/shared/npcs.json'),
      this._fetchJSON('story/shared/items.json'),
    ]);

    this.assembler = null;
    this.npcData = npcs ?? {};
    this.itemData = items ?? {};
  }
}
```

- [ ] **Step 4: 修改 loadScene 支持模板组装**

```javascript
async loadScene(sceneId) {
  if (this.sceneCache[sceneId]) {
    return this.sceneCache[sceneId];
  }

  const chapterId = this.gameState.get('currentChapter');
  let scene;

  if (this.assembler) {
    // 随机生成章节：从模板组装
    const templateUrl = `story/${chapterId}/templates/${sceneId}.json`;
    const template = await this._fetchJSON(templateUrl);

    if (!template) {
      throw new Error(`[StoryEngine] Template not found: ${sceneId}`);
    }

    scene = this.assembler.assemble(sceneId, template, this.gameState);
  } else {
    // 手写章节：直接加载
    const url = `story/${chapterId}/scenes/${sceneId}.json`;
    scene = await this._fetchJSON(url);
  }

  if (!scene) {
    throw new Error(`[StoryEngine] Scene not found: ${sceneId}`);
  }

  this.sceneCache[sceneId] = scene;
  return scene;
}
```

- [ ] **Step 5: 验证语法**

```bash
node --check js/engine/story-engine.js
```

- [ ] **Step 6: 提交**

```bash
git add js/engine/story-engine.js
git commit -m "feat(ch2): extend story-engine to support randomized chapters"
```

---

## Task 8: 章节切换逻辑

**Files:**
- Modify: `js/engine/story-engine.js`
- Modify: `js/app.js`

- [ ] **Step 1: 在 story-engine 中添加章节切换方法**

在 `story-engine.js` 的 `start()` 方法之前添加：

```javascript
/**
 * 切换到下一章：更新章节 ID、清空场景缓存、加载新章节、播放起始场景。
 * @param {string} nextChapterId
 */
async switchChapter(nextChapterId) {
  this.gameState.set('currentChapter', nextChapterId);
  this.sceneCache = {};
  await this.loadChapter(nextChapterId);

  const startScene = this.chapterMeta?.startScene ?? '01_arrival';
  this.gameState.set('currentScene', startScene);
  await this.playScene(startScene);
}
```

- [ ] **Step 2: 修改 _handleChoice 支持章节结束**

在 `_handleChoice` 方法中，当 `choice.target` 指向的场景属于下一章时（以 `chapter` 前缀判断），调用 `switchChapter`：

```javascript
async _handleChoice(choice) {
  if (choice.effects) {
    this.gameState.applyEffects(choice.effects);
  }

  this.renderer.updateStats({
    experience: this.gameState.get('stats.experience'),
    mood: this.gameState.get('stats.mood'),
  });

  this.saveManager.autoSave();

  if (choice.target) {
    // 检查是否需要切换章节（target 包含 chapter 前缀）
    const targetChapter = this._detectChapterSwitch(choice.target);
    if (targetChapter) {
      await this.switchChapter(targetChapter);
    } else {
      await this.playScene(choice.target);
    }
  }
}

/**
 * 检测是否需要章节切换。
 * 如果 target 格式为 "chapter2:scene_id"，返回目标章节 ID。
 * @param {string} target
 * @returns {string|null}
 */
_detectChapterSwitch(target) {
  if (target.includes(':')) {
    const [chapterId, sceneId] = target.split(':');
    return chapterId;
  }
  return null;
}
```

- [ ] **Step 3: 修改 12_departure.json 的 target 使用章节切换格式**

```json
{
  "text": "继续旅途（待续）",
  "target": "chapter2:01_arrival",
  "effects": { "experience": 3 },
  "condition": null
}
```

- [ ] **Step 4: 验证语法**

```bash
node --check js/engine/story-engine.js && node --check js/app.js
```

- [ ] **Step 5: 提交**

```bash
git add js/engine/story-engine.js story/chapter1/scenes/12_departure.json
git commit -m "feat(ch2): add chapter switching logic with chapter:scene target format"
```

---

## Task 9: 端到端验证

- [ ] **Step 1: 验证完整流程**

启动本地服务器：
```bash
python3 -m http.server 8000
```

在浏览器中测试：
1. 开始新游戏 → 第一章正常播放
2. 走完第一章 → 自动进入第二章
3. 第二章开局文本显示正常
4. 选择探索路径 → NPC 随机出现
5. 物品随机获取
6. 汇合节点 → 将军遗孀固定出现
7. 结局分支根据标记正确过滤
8. 存档/读档正常（跨章节）

- [ ] **Step 2: 验证随机性**

刷新页面重开新游戏，确认：
- NPC 分配不同
- 物品分布不同
- 文本内容因 NPC 不同而变化

- [ ] **Step 3: 最终提交**

```bash
git add -A
git commit -m "feat(ch2): chapter 2 complete - randomized generation system"
```

---

## 验收清单

- [ ] NPC 池 6 个角色数据完整
- [ ] 物品池 5 个物品数据完整
- [ ] 骨架定义 12 个节点
- [ ] 场景模板 12 个文件，全部 JSON 有效
- [ ] 场景组装器正确替换占位符
- [ ] NPC 随机分配不重复
- [ ] 物品随机分配不重复
- [ ] story-engine 正确检测 randomized 标记
- [ ] 章节切换逻辑正常
- [ ] 状态继承（mood/flags/items 影响开局）
- [ ] 结局条件选项正确过滤
- [ ] 存档/读档跨章节正常
